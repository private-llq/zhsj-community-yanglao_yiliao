server:
  port: 20003
spring:
  gateway:
    tokenUri: 192.168.12.49:4646/zhsj/base/api
  application:
    name: community-healthdata-service
  redis:
    database: 0
    host: 192.168.12.49
    password: text_profile
    port: 6379
    timeout: 7000
    jedis:
      pool:
        max-idle: 10000
        min-idle: 10
        max-active: -1
        max-wait: -1
  datasource:
    druid:
      username: root
      password: dev_zhsj_mysql
      url: jdbc:mysql://192.168.12.49/zhsj_community_yanglao_yiliao?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
      driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    auto-mapping-behavior: full
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath*:mapper/**/*Mapper.xml
  global-config:
    # 逻辑删除配置
    db-config:
      # 删除前
      logic-not-delete-value: 1
      # 删除后
      logic-delete-value: 0
eureka:
  client:
    service-url:
      defaultZone: http://192.168.12.49:36366/eureka
  instance:
    hostname: ${spring.cloud.client.ip-address}
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${spring.application.instance_id:${server.port}}
